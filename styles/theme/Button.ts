const Button = {
  baseStyle: {
    borderRadius: "lg",
  },
  sizes: {
    xs: {
      borderRadius: "md",
    },
    sm: {
      borderRadius: "lg",
    },
    md: {
      borderRadius: "xl",
    },
    lg: {
      borderRadius: "xl",
    },
  },
};

export default Button;
